import request from 'superagent'

const state = {
    cash: {
        CAD: 0
    },
    supplies: {
        bitpepsi: 0
    }
}

const mutations = {
    // match to events
}

const actions = {

}

module.exports = {
    state,
    mutations,
    actions
}
