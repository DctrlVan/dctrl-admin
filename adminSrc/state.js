// const db = require('./rethinkdbConnection')

const state = {
    members: [{name: 'test2'}],
    bounties: [{name: 'agg'}],
    dctrl: {
        cash : {
            CAD: 111
        },
        supplies: {
            bitpepsi: 110
        }
    },
}

module.exports = state
